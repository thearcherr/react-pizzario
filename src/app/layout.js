import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Pizzario - Restaurant in Naples",
  description: "Pizzario is a restaurant situated in the heart of Naples or Napoli. The restaurant offers a wide range of food including a variety of pizzas, french fries, burgers of varying variety, coffee and other traditional Italian food including our famous Cruncho Biscuits.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
